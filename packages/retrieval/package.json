{
  "name": "@aura/retrieval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AURA Hub retrieval & memory foundation — provider-independent interfaces + dummy engines. No embeddings, no indexing, no vector DB, no coupling to any provider/UI/runtime.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
