{
  "name": "@aura/workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AURA Workflow — orchestration domain model and persistence. Versioned workflow definitions (triggers → AI / capability tools / control / mission / output) and their runs. Definitions only: nothing here executes, polices or approves — the runtime phases route every executable node through the Capability Fabric invoke path.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "dependencies": {
    "@aura/capability-fabric": "*",
    "@aura/connected-environment": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}