{
  "name": "@aura/intelligence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AURA Hub intelligence foundation — a modular, provider-agnostic pipeline. Architecture only: no AI SDK, no API calls, no business logic.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
